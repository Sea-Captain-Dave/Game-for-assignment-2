extends Label


# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	text = "With the coolers destroyed\nThe computer core overheated\nfoiling Folivoras plans once again\nHowever with no computer core Unit-tr-"+str($"/root/MainHud".deaths)+"\nwent to sleep possable never to wake up"



# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
